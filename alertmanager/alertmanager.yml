# Alertmanager: принимает алерты от Prometheus и рассылает на email.
# Пароли/адреса берутся из переменных окружения (--config.expand-env),
# которые задаются в .env на машине-генераторе (см. .env.example).
global:
  resolve_timeout: 5m

route:
  receiver: email
  group_by: ["alertname", "instance"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: email
    email_configs:
      - to: "${ALERT_EMAIL_TO}"
        from: "${ALERT_EMAIL_FROM}"
        smarthost: "${SMTP_HOST}:${SMTP_PORT}"
        auth_username: "${SMTP_USER}"
        auth_password: "${SMTP_PASSWORD}"
        require_tls: true
        send_resolved: true
        headers:
          Subject: "[MUZEA] {{ .Status | toUpper }}: {{ .CommonLabels.alertname }} ({{ .CommonLabels.instance }})"
